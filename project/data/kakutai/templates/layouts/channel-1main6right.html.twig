

channelsUpdate = function( crect ) {

    channels = [];
    var ratio = 1.0 / renderer.getPixelRatio();
    var wide = crect.width * ratio;
    var high = crect.height * ratio;

    var smallwidth = wide * 0.25;
    var smallheight = high * 0.35;

    var mainwidth = wide - smallwidth;
    var mainheight = high - smallheight;

    channels.push( { top: smallheight, left: 0, width: mainwidth, height: mainheight } );

    // Four across the bottom
    for(var i=0; i<4; i++) {
        channels.push( { top: 0, left: i * smallwidth, width: smallwidth, height: smallheight } );
    }

    channels.push( { top: smallheight, left: wide-smallwidth, width: smallwidth, height: smallheight } );
    channels.push( { top: smallheight * 2.0, left: wide-smallwidth, width: smallwidth, height: smallheight } );

}
